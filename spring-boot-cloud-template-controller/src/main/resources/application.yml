spring:
  profiles:
    active: local
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
server:
  port: 8101
  tomcat:
    uri-encoding: UTF-8
    max-threads: 500
    max-connections: 100
  servlet:
    context-path: /template